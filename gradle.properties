# Память Gradle-демона (увеличено для предотвращения SIGTERM 143 при genSources)
org.gradle.jvmargs=-Xmx3072m -XX:+UseG1GC

# Параллельное выполнение и лимит воркеров для стабильности в контейнерах
org.gradle.parallel=false
org.gradle.workers.max=1

# IntelliJ IDEA Configuration Cache
org.gradle.configuration-cache=false

# Fabric Properties
minecraft_version=26.2
yarn_mappings=26.2+build.1
loader_version=0.19.5
loom_version=1.17.20

# Mod Properties
version=1.0.0
group=com.example

# Dependencies
fabric_api_version=0.160.0+26.2
